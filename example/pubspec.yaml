name: flutter_swr_example
description: "Runnable demo of flutter_swr's MVP features."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.2
  flutter_swr:
    path: ../
  http: ^1.2.0
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
